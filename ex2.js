//Function that enables the code to do the math
function addition(value) {
    console.log("Value is: " + value);
}
//Declaring Varibles and executing the function
var firstVal = 42;
var secondVal = 1;
var sumOfVals = (firstVal + secondVal).toLocaleString();
addition(sumOfVals);
